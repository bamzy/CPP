
#include "printReport.h"
void printReport(int counter, int total_ants, int nmin_ants, int nmax_ants)
{
	 // report the counter and value of total ants + min + max on a square
	std::cout << counter << " " << total_ants << " " << nmin_ants << " " << nmax_ants << std::endl;
}